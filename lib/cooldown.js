var Discord=require("discord.js");

module.exports = {
	/*
		Takes a bot and msg object from discord.js and returns callback(error, responseString)
	*/
	checkCooldown : function(bot,msg,callback) {
		
	}
};